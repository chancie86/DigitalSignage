<UserControl x:Class="Display.Views.ClockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Display.Controls"
             xmlns:viewModels="clr-namespace:Display.ViewModels"
             xmlns:clocks="clr-namespace:Display.ViewModels.Clocks"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type clocks:ClockViewModel}, IsDesignTimeCreatable=False}"
             d:DesignHeight="300" d:DesignWidth="300">
    <controls:AnalogueClock Time="{Binding Time}" Text="{Binding Text}" ClockName="{Binding Name}" IsDay="{Binding IsDay}" Width="400" Height="400" />
</UserControl>
